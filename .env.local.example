
# 🎭 Biospark Health AI - Local Development Environment Variables
# BMAD Phase 1 Complete System Configuration
# Created: July 23, 2025
# Status: PRODUCTION READY WITH FALLBACKS

# =============================================================================
# CORE APPLICATION SETTINGS
# =============================================================================
NEXT_PUBLIC_APP_ENV=development
NEXTAUTH_SECRET=your_secure_random_string_here_minimum_32_characters
NEXTAUTH_URL=http://localhost:3000

# =============================================================================
# DATABASE CONFIGURATION (PostgreSQL/Supabase)
# =============================================================================
# For production, use your actual PostgreSQL connection string
# For development, SQLite fallback will be used if not provided
DATABASE_URL="postgresql://username:password@hostname:port/database_name"
DIRECT_URL="postgresql://username:password@hostname:port/database_name"

# =============================================================================
# REDIS CONFIGURATION (OPTIONAL - CACHING LAYER)
# =============================================================================
# For production, use your actual Redis connection string
# For development, caching will be disabled if not provided
REDIS_URL="redis://localhost:6379"

# =============================================================================
# SENTRY MONITORING (OPTIONAL - ERROR TRACKING)
# =============================================================================
# For production, use your actual Sentry DSN
# For development, error tracking will be disabled if not provided
SENTRY_DSN="https://your-sentry-dsn@sentry.io/project-id"
NEXT_PUBLIC_SENTRY_DSN="https://your-sentry-dsn@sentry.io/project-id"
SENTRY_ORG="your-sentry-organization"
SENTRY_PROJECT="your-sentry-project"
SENTRY_AUTH_TOKEN="your-sentry-auth-token"

# =============================================================================
# AI & EXTERNAL INTEGRATIONS
# =============================================================================
OPENAI_API_KEY="sk-your-openai-api-key"
ZEP_API_KEY="your-zep-api-key"
ZEP_API_URL="https://api.getzep.com"
ZEP_ENCRYPTION_KEY="your-32-character-encryption-key-for-hipaa"

# =============================================================================
# PAYMENT PROCESSING (OPTIONAL)
# =============================================================================
STRIPE_SECRET_KEY="sk_test_your-stripe-secret-key"
STRIPE_PUBLISHABLE_KEY="pk_test_your-stripe-publishable-key"

# =============================================================================
# SECURITY & COMPLIANCE
# =============================================================================
ENCRYPTION_KEY="your-32-character-encryption-key-here"
JWT_SECRET="your-jwt-secret-key-minimum-32-characters"
PHI_ENCRYPTION_KEY="your-phi-encryption-key-for-hipaa-compliance"

# =============================================================================
# ANALYTICS & MONITORING (OPTIONAL)
# =============================================================================
GOOGLE_ANALYTICS_ID="GA-XXXXXXXXX"
MIXPANEL_TOKEN="your-mixpanel-token"

# =============================================================================
# EMAIL & NOTIFICATIONS (OPTIONAL)
# =============================================================================
SMTP_HOST="smtp.your-provider.com"
SMTP_PORT="587"
SMTP_USER="your-smtp-username"
SMTP_PASS="your-smtp-password"
FROM_EMAIL="noreply@your-domain.com"

# =============================================================================
# DEPLOYMENT CONFIGURATION
# =============================================================================
VERCEL_URL="http://localhost:3000"
NODE_ENV="development"

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env.local
# 2. Replace placeholder values with your actual credentials
# 3. For development, you can leave optional services as placeholders
# 4. The application will gracefully handle missing optional services
# 5. Never commit .env.local to version control

# =============================================================================
# FALLBACK BEHAVIOR
# =============================================================================
# - DATABASE_URL: Falls back to SQLite (file:./dev.db) if not provided
# - REDIS_URL: Caching disabled if not provided or contains placeholders
# - SENTRY_DSN: Error tracking disabled if not provided or contains placeholders
# - ZEP_API_KEY: Memory features disabled if not provided
# - All other services: Graceful degradation with warning messages

# =============================================================================
# SECURITY NOTES
# =============================================================================
# - Use strong, unique passwords for all services
# - Enable 2FA on all external service accounts
# - Regularly rotate API keys and secrets
# - Monitor access logs and usage patterns
# - Follow HIPAA compliance guidelines for healthcare data
